name: My First Workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest  # where its running 
        steps: 
            - name: Welcome message
                run : echo "My first Github Actions Job"

            - name: List files
                run: ls

            - name: Read File
                run: cat README.md
            

